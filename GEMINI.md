# 프로젝트 규칙 및 지침

## 진행 상황 자동 업데이트 규칙
- 모든 내용은 한글로 작성하세요.
- 각 작업 단계를 완료하거나 파일 구조, 핵심 로직을 변경할 때마다 반드시 이 `GEMINI.md` 파일의 [진행 상황] 섹션을 직접 수정하여 업데이트하세요.
- 완료된 항목은 체크(`[x]`)로 표시하고, 다음으로 진행할 작업(`[ ]`)을 명시하세요.
- 새로운 문제점이나 결정된 기술 스택이 생기면 관련 섹션에 즉시 기록하세요.

---

## 기술 스택 및 환경
- **OS**: Windows
- **언어/런타임**: HTML5, CSS3, JavaScript (Vanilla JS, ES6+)
- **프레임워크/라이브러리**: 없음 (순수 표준 기술만 사용)

---

## 프로젝트 구조
```text
Lab2_TodoApp/
├── GEMINI.md
├── index.html
├── style.css
└── app.js
```

---

## 진행 상황
- [x] `GEMINI.md` 규칙 및 프로젝트 가이드라인 파일 생성
- [x] **Step 1**: HTML/CSS 스캐폴딩 및 UI 뼈대 구축
  - [x] 상단 헤더: 앱 타이틀 및 전체 진행률 바(퍼센트 텍스트 및 시각적 바) 구성
  - [x] 입력 영역: 할 일 텍스트 input, 카테고리 select(개인/업무), 추가 버튼 구성
  - [x] 목록 영역: 할 일이 추가될 빈 `<ul>` 컨테이너 구성
  - [x] CSS 스타일링: 모던 UI 디자인, `.completed` 취소선/Dimmed 스타일, 카테고리 뱃지(`.badge`) 정의
- [x] **Step 2**: 데이터 스키마 및 LocalStorage 연동 로직 구현 (`app.js`)
  - [x] 상태 변수 `todos` 빈 배열 선언
  - [x] Todo 데이터 객체 스키마 정의 (`id`, `content`, `category`, `isCompleted`, `createdAt`)
  - [x] LocalStorage 저장 함수 `saveTodos()` 구현
  - [x] LocalStorage 파싱 및 예외 처리(Try-Catch) 함수 `loadTodos()` 구현
- [x] **Step 3**: 비즈니스 로직 (CRUD 함수) 작성 (`app.js`)
  - [x] `addTodo(content, category)` 구현 및 `saveTodos()` 연동
  - [x] `deleteTodo(id)` 구현 (`confirm()` 확인창 포함) 및 `saveTodos()` 연동
  - [x] `toggleTodoStatus(id)` 구현 및 `saveTodos()` 연동
  - [x] `updateTodo(id, newContent)` 구현 및 `saveTodos()` 연동
- [x] **Step 4**: DOM 렌더링 및 진행률(Progress Bar) 계산 로직 작성 (`app.js`)
  - [x] `renderTodos()`: XSS 방지(`document.createElement`, `textContent`) 적용 및 빈 상태 안내 문구 처리
  - [x] 각 `<li>` 항목에 체크박스, 텍스트, 카테고리 뱃지, 수정/삭제 버튼 조립 및 `.completed` 상태 반영
  - [x] `updateProgress()`: 달성률(%) 계산(할 일 0개 시 0%) 및 Progress Bar 너비/텍스트 반영
  - [x] `render()`: 메인 함수로 `renderTodos()`와 `updateProgress()` 순차 호출 통합
- [x] **Step 5**: 이벤트 리스너 바인딩 및 앱 초기화 (`app.js`)
  - [x] 입력 폼 이벤트: '추가' 버튼 클릭 또는 Enter 키 시 `addTodo()` 실행 및 공백 입력 시 `alert()` 방어
  - [x] 이벤트 위임(Event Delegation): `<ul>` 컨테이너에 단일 리스너를 등록해 체크박스, 수정, 삭제 일괄 처리
  - [x] 수정 모드 처리: '수정' 클릭 시 인풋 모드 전환 및 '저장'/'Enter' 키 입력 시 `updateTodo()` 실행
  - [x] 앱 초기화: `DOMContentLoaded` 시 `loadTodos()` 데이터 복원 및 `render()` 초기 화면 출력
- [x] **추가 기능 1**: "My Tasks" 브랜딩 변경 및 키워드 기반 자동 카테고리 분류 (`app.js`, `index.html`)
  - [x] 업무(`work`)/개인(`personal`) 키워드 사전(`CATEGORY_KEYWORDS`) 정의
  - [x] 인풋 실시간 감지(`classifyCategoryByKeyword`)를 통한 카테고리 자동 선택 및 펄스 애니메이션
  - [x] 폼 제출 시 키워드 자동 보정 추가
- [x] **추가 기능 2**: 화이트 / 노란색 / 하늘색 테마 전면 리뉴얼 (`style.css`)
  - [x] 배경 그라디언트(하늘색~화이트~노란색) 및 깨끗한 순백색 카드 레이아웃
  - [x] 하늘색 액션 버튼/포커스 링, 노란색-하늘색 진행률 바, 파스텔 톤 카테고리 뱃지
- [x] **추가 기능 3**: 데스크톱 2컬럼 대시보드 및 전면 반응형 UI 구현 (`index.html`, `style.css`, `app.js`)
  - [x] 데스크톱 화면(최대 1240px) 2컬럼 그리드(좌: 통계/입력/팁 카드, 우: 와이드 할 일 목록) 구축
  - [x] 실시간 진행률 통계 카운트(`0 / 0 완료`) 및 총 항목 뱃지 표시
  - [x] 태블릿(1023px 이하) 및 모바일(640px 이하) 화면 자동 스택 미디어 쿼리 최적화
- [x] **추가 기능 4**: 등록일 자동 기록, 캘린더 마감 시한, 진행 상황 메모 모달, 사이드바 일자별 활동 뷰 (`app.js`, `index.html`, `style.css`)
  - [x] 등록일(`createdAt`) 자동 포맷팅 표기 및 `<input type="date">` 캘린더 피커 연동
  - [x] D-Day(D-N, D-Day, 기한 초과) 실시간 계산 및 시각화 뱃지
  - [x] 진행 상황 세부 기록을 위한 메모 모달 팝업 및 `updateTodoMemo()` 로직 구현
  - [x] 사이드바 일자별(Daily) 등록/완료 건수 집계 및 날짜 클릭 시 필터링 뷰 기능
- [x] **추가 기능 5**: 연/월/일 활동 기록 필터(디폴트: 해당 월, 귀여운 반응형 UI) 및 장기 프로젝트 계층형 관리(개요, 대/중분류, 하위 일일 태스크 및 메모 유지)
  - [x] 사이드바 연/월/일 필터 칩 탭(`[월별 🗓️]`, `[일별 📅]`, `[연도별 🌟]`) 및 기간 네비게이터(`◀`, `▶`, `오늘 🐾`)
  - [x] 디폴트 모드로 현재 연/월의 활동 이력 필터링 및 귀여운 파스텔 젤리 캡슐 디자인 적용
  - [x] 메인 뷰 전환 탭(`[📋 데일리 할 일]`, `[🚀 장기 프로젝트]`, `[🌟 전체 모아보기]`) 및 프로젝트 생성 모달 구현
  - [x] 프로젝트 개요, 대분류/중분류별 하위 태스크 인라인 생성, 완료율 실시간 게이지 및 기존 메모/마감일 기능 100% 연동
- [x] **추가 기능 6**: 프로젝트 아코디언 접기/펼치기(디폴트 접힘), [관리] 드롭다운(수정/삭제), 대분류(필수)/중분류(선택) 동적 행 UI (`index.html`, `style.css`, `app.js`)
  - [x] 프로젝트 아코디언 접기/펼치기: 기본 상태는 프로젝트명, 개요, 진행률만 깔끔하게 노출되는 접힘(collapsed) 모드 제공
  - [x] `[관리 ⚙️]` 팝오버 드롭다운 메뉴: 프로젝트 카드 우측에 관리 버튼 배치, 하위 메뉴로 `✏️ 프로젝트 수정` 및 `🗑️ 프로젝트 삭제` 지원
  - [x] 프로젝트 정보 수정 모달: 프로젝트명, 개요, 대분류 및 중분류를 실시간 수정 및 동기화
  - [x] 대분류(필수) / 중분류(선택) 동적 입력 행(Row) UI: 나열식이 아닌 한 칸씩 추가/삭제 가능한 인터페이스 (`[+ 분류 추가 ➕]`, `[❌]`) 구축
- [x] **추가 기능 7**: 프로젝트 생성 시 대/중분류 회색 예시(placeholder) 힌트 및 입력 시 자동 소멸 구현 (`app.js`, `style.css`)
  - [x] 프로젝트 생성 모달 열기 시 기본값 행을 빈 value로 유지하고 `(예시) 기획 및 설계`, `(예시) 화면 기획` 등의 placeholder로 제공
  - [x] CSS `::placeholder`에 은은한 회색(`#94a3b8`) 스타일을 적용하여 힌트 상태를 시각적으로 명확히 구분
  - [x] 사용자가 키보드로 타이핑을 시작하면 회색 예시가 자동으로 사라지고 실제 입력값으로 치환되도록 구현
  - [x] `[+ 분류 추가 ➕]` 버튼 클릭 시에도 회색 예시 힌트 placeholder 자동 적용
- [x] **추가 기능 8**: 모바일 뷰포트 맞춤 및 배경-기능 컨테이너 사이즈 불일치(가로 스크롤/축소 버그) 전면 재정비 (`index.html`, `style.css`)
  - [x] `index.html`: `viewport-fit=cover` 및 `maximum-scale=1.0, user-scalable=no` 뷰포트 메타태그 강화로 모바일 WebKit 자동 축소(Zoom-out) 방지
  - [x] `style.css`: `html, body`에 `width: 100%; overflow-x: hidden;` 및 `align-items: flex-start;` 적용하여 스크롤 및 레이아웃 안정화
  - [x] `.app-container`: 모바일 환경에서 100% 폭과 적정 패딩(`16px 12px`)으로 배경과 기능 카드가 완벽하게 맞물리는 카드형 앱 핏 구현
  - [x] 모바일 반응형 요소 전면 개편: 메인 탭 3분할 균등 그리드, 프로젝트 헤더/게이지/버튼 유동 배치, 할 일 카드 텍스트-액션 버튼 2열 분리 레이아웃 적용
- [ ] 후속 작업: 추가 기능 확장 또는 사용자 피드백 대응

---

## 이슈 및 결정 사항
- **[2026-09-16]** 프로젝트 초기화: 자동 업데이트 규칙을 준수하도록 `GEMINI.md` 기본 템플릿 설정 완료.
- **[2026-09-16]** Step 1 완료: 외부 라이브러리 없이 순수 HTML/CSS로 모던한 카드형 UI 스캐폴딩 완료. 완료 상태(`.completed`) 및 카테고리 뱃지(`.badge-personal`, `.badge-work`) CSS 클래스 사전 정의 완료.
- **[2026-09-16]** Step 2 완료: DOM 조작 없이 순수 상태 변수(`todos`)와 LocalStorage 입출력 및 JSON 파싱 에러 방지를 위한 Try-Catch 예외 처리 로직(`saveTodos`, `loadTodos`) 구현 완료.
- **[2026-09-16]** Step 3 완료: 화면 렌더링과 분리된 순수 데이터 조작 함수(`addTodo`, `deleteTodo`, `toggleTodoStatus`, `updateTodo`) 작성 완료. 모든 CRUD 함수 실행 시 `saveTodos()`가 자동 호출되도록 구현 완료.
- **[2026-09-16]** Step 4 완료: XSS 방지를 위해 순수 DOM API(`document.createElement`, `textContent`)를 사용한 `renderTodos()`, 완료율을 실시간 계산해 Progress Bar 너비와 텍스트를 업데이트하는 `updateProgress()`, 그리고 둘을 통합 실행하는 `render()` 구현 완료.
- **[2026-09-16]** Step 5 완료: 이벤트 위임(`todoList.addEventListener`)을 활용한 고효율 리스너 바인딩, 동적 인풋 전환 인라인 수정 모드, 폼 제출 및 초기화(`DOMContentLoaded`)까지 전체 5단계 애플리케이션 완성.
- **[2026-09-16]** 키워드 자동 분류 & 신규 테마 완료: "My Tasks" 앱 타이틀 적용, 타이핑 시 키워드 감지(업무/개인)를 통한 카테고리 셀렉트 자동 전환 및 하이라이트 효과 구현, 화이트/노란색/하늘색 컬러 테마 전면 리디자인 완료.
- **[2026-09-16]** 데스크톱 대시보드 & 반응형 UI 완료: 좁은 모바일 카드 제약을 탈피해 최대 1240px의 와이드 데스크톱 2컬럼 레이아웃(좌측 컨트롤 패널 + 우측 스크롤 리스트) 구축 완료. 태블릿 및 모바일 브레이크포인트 최적화 완료.
- **[2026-09-16]** 날짜/메모/일자별 활동 뷰 완료: 등록일 자동 보존, `<input type="date">` 캘린더 피커 및 D-Day 뱃지, 세부 진행 메모 모달창, 사이드바 일자별 [등록 N건 | 완료 M건] 통계 및 필터링 기능 구현 완료.
- **[2026-09-16]** 연/월/일 귀여운 필터 & 장기 프로젝트 관리 완료: 사이드바에 연/월/일 필터 칩 및 기간 네비게이터(기본값: 해당 월) 구축, 귀여운 파스텔 톤 반응형 스타일링 완료, 장기 프로젝트 개요 및 대/중분류별 하위 태스크 생성/완료/메모 연동 기능 완성.
- **[2026-09-16]** 프로젝트 아코디언 & 관리/수정 메뉴 & 동적 대/중분류 완료: 프로젝트 카드를 기본 접힘(collapsed) 상태로 깔끔하게 정리하고 토글 지원, [관리 ⚙️] 드롭다운을 통해 프로젝트명/개요/분류 일괄 수정 및 삭제 지원, 대분류(필수)/중분류(선택) 구조를 한 행씩 동적으로 추가/삭제할 수 있는 UI 구현 완료.
- **[2026-09-16]** 프로젝트 생성 시 대/중분류 회색 예시(Placeholder) 및 자동 소멸 구현: 생성 모달에서 고정 value 대신 회색(`color: #94a3b8`) 예시 텍스트(placeholder)를 제공하여 사용자가 백스페이스로 기존 텍스트를 지울 필요 없이 바로 타이핑을 시작하면 예시가 자연스럽게 사라지도록 UX 개선 완료.
- **[2026-09-16]** 모바일 배경-컨테이너 사이즈 불일치 버그 해결: 모바일 WebKit 브라우저에서 일부 하위 요소(프로젝트 헤더, 탭 버튼)의 고정 폭으로 인해 뷰포트가 비정상 축소(Zoom-out)되어 우측 배경이 비고 컨테이너가 좁아지던 문제를 `overflow-x: hidden`, `viewport-fit=cover`, 미디어 쿼리 100% 핏 전면 최적화를 통해 완벽히 해결 완료.
